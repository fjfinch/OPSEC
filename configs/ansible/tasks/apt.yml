- block:

  - name: 'APT : install tools'
    apt:
      name: '{{ item.name }}'
      state: '{{ item.state }}'
    loop:
      - { name: terminator, state: latest }
      - { name: wireshark, state: latest }

  become: true
