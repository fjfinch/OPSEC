--- command
ansible-playbook main.yml -i inventory.ini -K


--- backup
# check become
    - name: test1
      ansible.builtin.shell: whoami
      register: command_output
    - debug:
        var: command_output.stdout_lines


--- todo
fix paths : relative vs absolute : ~/test vs /home/user/test
debug
apt install : latest?
single/double quotes
no internet fail
remove other same installed tools

community.general.git_config:
community.crypto.openssh_keypair:
community.general.pipx:
copy:
apt:
lineinfile:
blockinfile:

ansible-galaxy collection install community.mysql
ansible-galaxy collection install community.general
[WARNING]: Collection community.general does not support Ansible version 2.10.8

enum4linux-ng

COMMANDS:
source zshrc
pipx autocomplete
git submodule update --init
