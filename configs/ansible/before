sudo apt update && sudo apt upgrade -y && sudo apt install -y python3-pip pipx git && pipx install ansible --include-deps && pipx ensurepath


sudo apt update
sudo apt upgrade -y

sudo apt install -y python3-pip pipx git

pipx install ansible --include-deps
pipx ensurepath

source ~/.zshrc
source ~/.bashrc
